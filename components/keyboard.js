export function Keyboard({keyboardLetter, setKeyboardLetter}) {

  // I think each individual letter needs its own component... for changing color
  return (
    <div>
      <p>Where the keyboard will go</p>
    </div>
  )
}